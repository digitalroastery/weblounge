steal.plugins('jquery/controller').then(function($) {

	/*
	 *
	 */
	$.Controller('Editor.Resourceview',
	{
		defaults: {
			resources: {},
			resourceType: 'pages',
		}
	},
	{	
		init: function(el) {
			
		}
	});

});
